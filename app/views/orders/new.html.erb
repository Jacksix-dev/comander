<%= simple_form_for [@table, @order] do |f| %>
  <%= f.input :selected_food_ids, collection: @foods, label: 'drink-food',input_html:{multiple:true} %>
  <%= f.input :waiter_id, collection: @waiter %>
  <%= f.button :submit %>
<% end %>
